#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
  * main - Entry point
  *
  * Return: Always 0.
  */
int main(void)
{
	int v;

	for (v = 0; v <= 9; v++)
	putchar(v);
	putchar('\n');
	return (0);
	}
